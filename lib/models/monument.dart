class Monument {
  String name, summary, map, mapLink, description;
  List<String> images;

  Monument(
      {required this.name,
      required this.summary,
      required this.map,
      required this.mapLink,
      required this.description,
      required this.images});
}
